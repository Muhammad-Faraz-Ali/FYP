import {createContext} from 'react';

export const GradesAndSubjectsContext = createContext();
export const NotificationsContext = createContext();
export const TestingContext = createContext(); //this context is useless just for testing purposes
